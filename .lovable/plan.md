# Final Badaie client, social, and brand correction

## Changes
- Update `/clients` to show the six verified client names exactly as supplied, using the current page structure and the existing approved combined client-logo asset without adding project claims.
- Keep one footer social group with exactly YouTube, LinkedIn, Facebook, TikTok, X, and WhatsApp Channel, all using the supplied URLs and opening in new tabs.
- Add the same six official accounts to the Contact page so they remain accessible there, without changing the existing enquiry form or contact details.
- Replace the current navy/gold/sand theme tokens with colors sampled from the official Badaie logo, preserving the current layouts, components, interaction states, contrast, and responsive behavior.
- Update the organization metadata to match the same six official social accounts.

## Verification
- Check the six client names and approved logo strip on `/clients`.
- Check social labels, URLs, uniqueness, and new-tab behavior in the footer and Contact page.
- Verify desktop and mobile rendering, navigation, images, and existing interactions.
